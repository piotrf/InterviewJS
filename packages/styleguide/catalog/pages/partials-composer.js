import React from "react";
import { markdown, ReactSpecimen } from "catalog";

export default () => markdown`

  ## TBC

  ${(
    <ReactSpecimen>
      <span>TBC</span>
    </ReactSpecimen>
  )}

`;
