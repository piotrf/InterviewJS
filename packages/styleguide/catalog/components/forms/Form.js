import css from "styled-components";
import {} from "prop-types";

import {} from "../../../utils";

const Form = css.form`
  margin-left: auto;
  margin-right: auto;
  max-width: 660px;
  position: relative;
`;

Form.propTypes = {};

Form.defaultProps = {};

export default Form;
